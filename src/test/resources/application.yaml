cron:
  syncRate: 300000

### Server properties ###
server:
  port : 9000
  use-forward-headers: true


server.servlet:
  use-forward-headers: true


logging:
  file: logs\irsync.log
  level:
    root: INFO

spring:
  output:
  ansi:
    enabled: DETECT

### Spring properties ###
spring.main.banner-mode: 'off'
spring.metrics.web.server.auto-time-requests: true

### Metrics ###
management.endpoints.web.exposure.include: info, health, metrics, prometheus, conditions
management.metrics.export.prometheus.enabled: true

rina.tenants:
  - institutionId: NO:NAVT007
    casBaseUrl: http://e34apvw007.devillo.no/eessiCas
    cpiBaseUrl: http://e34apvw007.devillo.no/eessiRest
    adminuser: supervisor
    adminpwd: supervisor123

logging.level.no.nav.eux: DEBUG